name: Run Tests
on: workflow_dispatch
jobs:
  test:
   runs-on: ubuntu-latest
   steps:
   - uses: actions/checkout@v3
   - name: Install update
     run: apt update
   - name: Install dependencies
     run: npm install
   - name: Run tests
     run: npm test
